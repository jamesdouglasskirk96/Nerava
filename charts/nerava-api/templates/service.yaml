apiVersion: v1
kind: Service
metadata:
  name: {{ include "nerava-api.fullname" . }}
  labels:
    {{- include "nerava-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nerava-api.selectorLabels" . | nindent 4 }}
